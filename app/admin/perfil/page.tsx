import { AdminAreaPage } from "@/components/admin/admin-area-page";

export const metadata = {
  title: "Perfil - Admin GEEF",
};

export const dynamic = "force-dynamic";

export default function PerfilPage() {
  return <AdminAreaPage />;
}
